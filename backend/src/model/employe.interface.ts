export interface Employe {
    first_name: string;
    last_name: string;
    email: string;
    number: string;
    gender: string;
    photo: string;
};

export interface IEmploye{
    id: string
};